{
    "name": "@screenview/native",
    "private": true,
    "version": "0.0.0",
    "description": "Native bindings to screen, keyboard, and mouse APIs",
    "scripts": {
        "build": "rm -f index.node && cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
        "build-debug": "yarn build --",
        "build-release": "yarn build -- --release",
        "node:test": "yarn run build && node index.js",
        "install": "yarn run build-release",
        "test": "cargo test"
    },
    "devDependencies": {
        "cargo-cp-artifact": "^0.1"
    }
}
